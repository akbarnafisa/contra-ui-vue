/**
 * @type {Array<string>}
 */
export const CButtonVariant = [
  'primary',
  'secondary',
  'tertiary',
  'destructive',
]
export enum CButtonVariantTypes {
  PRIMARY = 'primary',
  SECONDATY = 'secondary',
  TERTIARY = 'tertiary',
  DESTRUCTIVE = 'destructive',
}

/**
 * @type {Array<string>}
 */
export const CButtonType = ['button', 'submit', 'reset']

export enum CButtonTypes {
  BUTTON = 'button',
  SUBMIT = 'submit',
  RESET = 'reset',
}

/**
 * @type {Array<string>}
 */
export const CButtonSize = ['small', 'large']
export enum CButtonSizeTypes {
  SMALL = 'small',
  LARGE = 'large',
}
